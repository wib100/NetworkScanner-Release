# Network Scanner 0.3g

- Refined device list badges and indicators so dense rows stay readable without overlap
- Replaced the Alias text chip with a compact tag icon to reduce visual clutter
- Expanded demo mode so NEW, GONE, alias, and note states are visible for screenshots and presentations
- Deep scan is now enabled by default and no longer needs a separate settings toggle
- Improved the in-app update flow on modern Android, including better handling for notification and APK install permissions
